# Phase M.6 — Export Job Invariants

1. Every export job references exactly one export request
2. Jobs are async and non-blocking
3. Job execution is isolated
4. Job failure does not affect snapshot validity
5. Job history is auditable

