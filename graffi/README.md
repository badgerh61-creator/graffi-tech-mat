Graffi Option-Ω (Omega) Helper
==============================

Drop this graffi/ package and graffi_helper.py into the root of your Graffi-Tech-Mat repo.

Run a doctor scan like so:
    python graffi_helper.py doctor

The report will be in .graffi_helper_model/doctor_report.json
