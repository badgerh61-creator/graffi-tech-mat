# Tier 7.22 — Gizmo Payload Parity (Multi-Select + Pivot + BBox)

## Purpose (LOCKED)
Ensure gizmo-generated tool payloads match TransformToolPanel payload semantics.

## Payload Requirements
On commit, gizmo must include:
- target_id (typed selection primary)
- selected_target_ids (stable sorted ids) when multi
- pivot_mode / pivot when multi (custom requires pivot xyz)
- selection_bbox when multi (UI metadata)

## Invariants
- Deterministic ordering
- No guessing
- Governed execution path unchanged (Tool Adapter, proposals mode)
