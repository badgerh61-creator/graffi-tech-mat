def render_snapshot(scene_state: dict, render_profile: str) -> str:
    """
    Phase J.1 stub.
    Must behave like a pure function.
    Returns a placeholder image URL.
    """

    # ⚠️ NO engine mutation
    # ⚠️ NO filesystem writes
    # ⚠️ NO AI

    return f"https://placehold.co/512x512?text={render_profile}"

