# Tier 6G.12 — Instance / Clone Support (UI + Scene Index Compatibility)

## Purpose (LOCKED)
Support repeated parts (instances/clones) in the scene viewer with stable instance IDs,
deterministic selection IDs, and memory-efficient reuse of assets.

## Inputs
GET /snapshots/{snapshot_id}/scene (Tier 6G.1)
objects[] may now include optional "instances" list:

Object (extended):
{
  "id": string,
  "kind": string,
  "name": string,
  "asset_ref": string|null,
  "transform": { position, rotation, scale },
  "instances": [
    {
      "instance_id": string,
      "name": string|null,
      "transform": { position, rotation, scale }
    }
  ]
}

## Rendering Rules
1) If object.instances is empty or missing:
   - render single object as before
2) If instances exist:
   - treat base object as a template (asset_ref + base transform)
   - render one group per instance:
     group name: "obj:{object_id}@{instance_id}"
   - apply:
     finalTransform = baseTransform ∘ instanceTransform (compose)
   - each instance must be pickable independently

## Canonical Selection ID (LOCKED)
selectedId = "{object_id}@{instance_id}::{mesh_path}"
If no instance_id (non-instanced object):
selectedId = "{object_id}::{mesh_path}"

Object-only selection is allowed:
- "{object_id}@{instance_id}::"
- "{object_id}::"

## Determinism
- object_id and instance_id come from scene index
- mesh_path computed deterministically (6G.5)
- object groups must have stable names

## Invariants
- Read-only: no backend writes required for this tier
- No snapshot mutation
- No auto-instance creation

## Forbidden
- generating random instance IDs in viewer
- merging instances into one selection namespace
