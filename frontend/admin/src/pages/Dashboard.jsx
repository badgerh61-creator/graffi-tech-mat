import React,{useEffect,useState} from 'react'; export default function Dashboard(){ const [assets,setAssets]=useState([]); useEffect(()=>{ fetch('/assets').then(r=>r.json()).then(setAssets) },[]); return (<div style={{padding:20}}><h2>Assets</h2><table border='1' cellPadding='6'><thead><tr><th>ID</th><th>Filename</th><th>Processed</th></tr></thead><tbody>{assets.map(a=>(<tr key={a.id}><td>{a.id}</td><td>{a.filename}</td><td>{String(a.processed)}</td></tr>))}</tbody></table></div>); }
