# Tier 7.17 — Typed Selection UX Migration

## Purpose (LOCKED)
Migrate selection UI surfaces to display and rely on typed selection primary
as the canonical “active target”.

## Invariants
1. UI displays and highlights selection from typed selection store.
2. Tools must not read legacy selectedId for target resolution.
3. Legacy store remains only for backward compatibility (temporary).

## Exit Criteria
- ViewportSurface highlights typed primary
- TransformToolPanel displays typed target (from preflight)
- Selection HUD exists (debug-friendly)
